const Layout = ({children}) => {
    return (
        <>
            <div className={'nav'}>

            </div>
            <main>
                {children}
            </main>
        </>
    )
};

export default Layout